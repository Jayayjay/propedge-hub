python-dotenv>=1.0.0
requests>=2.31.0
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Windows-only — skipped automatically on Linux/Mac.
# On Linux, mt5_client.py falls back to MockMT5 (simulation).
# For real data on Linux, use the MQL5 EA (PropEdgeHub_Bridge.mq5) instead.
MetaTrader5>=5.0.45; sys_platform == "win32"
